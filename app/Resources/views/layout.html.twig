<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}POL Carnet{% endblock %}</title>

  {% block stylesheets %}
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  {% endblock %}
</head>

<body>
  <div class="container">
    <div id="header" class="jumbotron">
    <h1> Mon carnet d'adresses </h1>
    <p> Ceci est mon test Symfony2 pour la société Appartoo </p>
    </div>

    <div class="row">
      <div id="menu" class="col-md-3">
        <ul class="nav nav-pills nav-stacked">
          <li><a href="{{ path('pol_carnet_home') }}">Accueil</a></li>
          {% if app.user %}
          <li><a href="{{ path('pol_carnet_add') }}">Ajouter un contact</a></li>
          {% endif %}
        </ul>
        {% if app.request.uri != url('fos_user_registration_register') %}
          {% render url('fos_user_security_login') %}
        {% endif %}
      </div>
      <div id="content" class="col-md-9">
        {% block body %}
        {% endblock %}
      </div>
    </div>

    <hr>

    <footer>
      <p>Alexandre Pol © {{ 'now'|date('Y') }}</p>
    </footer>
  </div>
</body>

</html>
